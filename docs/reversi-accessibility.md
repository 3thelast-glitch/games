# Reversi accessibility

The Reversi board uses semantic buttons inside a grid. Each square announces its state and coordinates, legal moves remain keyboard-focusable, and the board uses fixed LTR coordinates so Arabic RTL layout does not mirror game geometry. The interface also respects reduced-motion preferences by disabling flip animation and transition effects.
